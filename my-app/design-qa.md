# AI 招聘重构 Design QA

## 检查范围

- 参考原型：`AI Recruitment Redesign Project/工作台 Workspace.dc.html`、`AI Recruitment Redesign Project/岗位管理 Positions.dc.html`、`AI Recruitment Redesign Project/候选人 Candidates.dc.html`、`AI Recruitment Redesign Project/人才库 TalentPool.dc.html` 及同目录设计说明。
- 实现地址：`http://localhost:3000/`
- 检查环境：桌面端、浅色模式；原型基准为 1920 × 1080，人才库应用内浏览器运行截图为 1394 × 775，并使用相同 1394 × 775 左上视口裁切原型进行对照。
- 已完成页面：工作台、岗位管理（列表、详情、新建、编辑及关联弹窗）、候选人（列表、看板、详情全部页签、内嵌表单与候选人专属弹窗）、人才库（统计、筛选、分组列表、分页、批量操作、上传、直属确认弹窗与详情抽屉）。
- 左侧菜单右下角折叠/展开图标按用户要求保留并使用新风格，但不作为视觉对比项；除该图标外，其余框架与页面均正常检查。

## 工作台

### 视觉检查

- 已对照原型检查页面结构、蓝色主色、文字层级、卡片、边框、圆角、阴影、间距、按钮、空状态与列表密度。
- 修复空的状态分布区域仍占用高度、导致最新候选人内容被挤出首屏的问题。
- 对照证据：`.design-qa/workspace/00-workspace-source.png`、`.design-qa/workspace/03-workspace-implementation-fixed.png`、`.design-qa/workspace/04-workspace-comparison-fixed.png`。

### 交互检查

- 设置入口、简历上传、搜索与刷新状态可用。
- 简历上传说明和文件选择范围已与后端实际支持格式统一为 PDF、DOCX。
- 空数据、加载中和有数据状态均使用同一套新设计语言。

## 岗位管理

### 页面与状态覆盖

- 岗位列表：统计、搜索、状态筛选、组织筛选、创建人筛选、刷新、新建、上传、设置、行操作及分页/横向信息承载。
- 岗位详情：候选人、岗位信息、JD 工作台、版本与发布四个页签。
- 岗位表单：新建和编辑完整字段、底部操作区、未保存确认。
- 关联弹窗：JD 配置、测评配置、基础/结构化方案、岗位上传、删除确认、发布预演。
- 候选人：列表、展开详情及岗位下候选人入口。

### 视觉检查

- 已对照原型检查页面框架、信息层级、颜色 token、字体、间距、圆角、阴影、表格、表单、标签、按钮、弹窗、选中/悬停、空状态和加载状态。
- 保留系统真实字段、状态、组织结构、权限和业务操作；未复制原型中的业务字段。
- 对照证据：`.design-qa/positions/00-positions-list-source.png`、`.design-qa/positions/21-positions-list-final.png`、`.design-qa/positions/22-positions-list-comparison.png`、`.design-qa/positions/23-position-detail-info-final.png`。

### 业务与交互修复

- 岗位详情上传会绑定当前岗位；列表上传继续使用智能识别模式。
- JD 版本记录不再展示内部系统提示词，只展示用户可理解的版本说明和 JD 内容。
- 发布能力当前后端仅为模拟实现，界面统一为“发布预演”，不再提供会造成误解的 API/RPA 真发布选项，也不展示模拟地址。
- 上传重复处理仅保留后端实际支持的跳过与覆盖。
- 手工编辑 JD 后，切换页签、返回或关闭时均会触发未保存确认，避免草稿静默丢失。
- 列表筛选在进入详情再返回后保持；岗位编号已纳入搜索。
- 测评方案新增入口按真实管理权限显示，避免无权限用户进入必然失败的操作。
- 岗位表单组织名称来自系统真实组织数据，移除硬编码文案。

## 候选人

### 页面与状态覆盖

- 主页面：招聘流程阶段、岗位范围、状态/匹配度/来源/时间/关键词筛选、高密度列表、批量选择、批量操作、空状态、加载状态、分页及响应式布局。
- 候选人详情：原始/标准/历史简历、测评、筛选、部门评审、面试题与面试安排、Offer、背调与跟进、右侧流程和快捷操作。
- 候选人专属弹窗：智能上传简历、部门评审、AI 完整输出、导出、批量绑定岗位、批量修改状态、单人状态确认、候选人/批量/简历删除、Offer/跟进/面试删除、发送简历邮件。
- 原型未覆盖的面试、Offer、跟进和权限状态均使用同一颜色、4/6/8px 圆角、控件密度和弹窗规则外推；未新增虚构业务。

### 视觉与业务检查

- 主列表改为原型式高密度表格，保留系统真实状态推导、组织、联系方式、期望城市、标签、AI 转岗建议、状态原因、原始文件和邮件摘要的可访问信息；空数据仍保留完整表头与分页结构。
- 详情使用右侧抽屉结构，保留真实字段、评审任务作用域、`visible_sections`、SSE 运行状态、面试 `round_index`、Offer、跟进和简历业务。
- 看板按推导后的展示状态分组，不再按原始状态造成流程错位；Offer、已入职等终态不会误触重新初筛。
- 候选人、流程、评审、面试、技能、邮件、日志、助手和人才库操作已接入现有细粒度权限；未授权入口不再暴露。
- 批量筛选/通过/淘汰仅允许当前页且尚未进入部门评审、面试或 Offer 的候选人，避免把后期候选人回退到初筛。
- 原生 `window.confirm/window.alert` 已全部替换为新风格确认弹窗或系统提示；详情内二级弹窗不再触发 Radix 焦点循环。
- 智能上传保留三种上传模式、来源、重复策略、城市识别、拖拽、并发上传、取消和人才库跳转，并修正并发批次总进度计算。
- 对照证据：`.design-qa/candidates/21-candidates-list-comparison-final.jpg`、`.design-qa/candidates/19-candidate-detail-comparison.jpg`、`.design-qa/candidates/11-upload-dialog-final.png`、`.design-qa/candidates/15-review-dialog.png`、`.design-qa/candidates/16-offer-form.png`。

### 交互检查

- 已通过岗位关联候选人入口打开真实候选人详情，逐项检查简历、筛选、评审、面试、Offer、背调、状态确认和删除流程。
- 当前 QA 账号候选人接口和列表均显示 4 条真实数据，列表行、总数、阶段计数及分页范围一致。
- 上传模式切换、高级选项、指定岗位、关闭草稿、详情内弹窗和取消路径均已实际操作验证。
- 最后一轮浏览器检查无新增 error/warning。

### 顶部与职位栏最终校准

- 候选人页不再渲染模块级返回/标题/组织切换/新增招聘需求顶部条；“上传简历”已移动到招聘阶段 Tab 行最右侧，且该行不再显示刷新等额外动作。
- 已移除筛选区的“全部岗位”下拉和列表/宫格切换；左侧“全部职位”继续作为真实岗位范围筛选入口。
- 左侧“招聘中职位”默认宽度为 250px，使用 20px 顶部留白、`#EBEEF5` 虚线分隔、32px 搜索框和 36px 职位项，与候选人原型一致。
- “招聘中职位”栏保留原有横向缩放能力：默认 250px，可在 18–420px 间拖拽；缩到 18px 时内容折叠且拖拽轨道保留，可随时向右重新展开，双击恢复 250px，键盘 Home/End 与左右方向键可用。
- 已实际验证鼠标移入“简历初筛”文字区域会自动展开“全部 / 未处理 / 进行中 / 本轮淘汰”状态菜单，无需点击角标。
- “上传简历”按钮可正常打开现有智能上传弹窗；页面运行日志无 error/warning。
- 对照证据：`.design-qa/candidates/22-candidates-top-final.png`、`.design-qa/candidates/23-candidates-top-comparison.jpg`。

### 主列表字段宽度校准

- 首轮发现 P1：表头和数据行前九列均为固定宽度，只有“操作”列使用 `1fr`，导致宽屏剩余空间全部进入操作列，按钮后出现约 300px 无效空白。
- 已将表头和虚拟列表行统一到同一套内容权重模板：候选人与岗位获得更高弹性，状态、来源、学历、工作年限和时间按内容长度分配；操作列按是否拥有发送简历权限固定为 168px / 216px，不再吸收剩余宽度。
- 宽屏运行测量：列表行宽 1283px，十列宽度依次为 40 / 246 / 169 / 119 / 126 / 105 / 77 / 91 / 141 / 168px，操作列右边界距内容区右侧约 10px。
- 窄屏运行测量：内容区 854px 时表格保持 1192px 最小宽度并提供横向滚动，字段不会被压缩到不可读。
- 匹配度排序入口保留；已移除不再生效的单列拖拽手柄，避免用户误以为自动权重表格仍支持手工改列宽。
- 对照证据：`.design-qa/candidates/24-candidates-columns-fluid.png`、`.design-qa/candidates/25-candidates-columns-wide.png`、`.design-qa/candidates/27-candidates-columns-comparison.jpg`。

## 验证结果

- `npm run build`：通过。
- `npm run typecheck`：通过。
- 本轮相关文件 ESLint（errors only）：通过，0 error。
- `git diff --check`：通过。
- 开发服务器重启后重新加载岗位管理，浏览器控制台无新增 error/warning。
- 候选人主列表、真实详情和嵌套弹窗检查后，浏览器控制台无新增 error/warning。

## 人才库

### 对照目标与证据

- 视觉真值：`AI Recruitment Redesign Project/人才库 TalentPool.dc.html`，原始截图 `.design-qa/talent-pool/00-talent-pool-source.png`。
- 运行实现：`http://localhost:3000/` 的人才库模块，截图 `.design-qa/talent-pool/01-talent-pool-implementation.png`。
- 全视图同屏证据：`.design-qa/talent-pool/02-talent-pool-comparison.png`；重点区域证据：`.design-qa/talent-pool/03-talent-pool-focus-comparison.png`。
- 状态：原型使用示例分组数据；最初检查时实际 QA 数据库为空，后续新版详情复核时已使用数据库现有的 1 条真实人才数据检查分组行和详情抽屉，未为截图制造业务数据。

### 必查设计面

- 字体与层级：沿用 Inter / PingFang SC 字体栈；标题 18px/600、统计数字 28px/600、正文与操作 12px、候选人姓名 13px，截断和行高与原型一致。
- 间距与布局：页面内边距 20px 32px 48px；页头、统计卡、筛选、分组头、56px 行和分页均按原型节奏实现。候选人、标签/说明、来源、时间和操作列使用内容权重铺满，窄桌面只在分组列表内部保留最小宽度与横向滚动，不再让整个页面隐藏右侧固定操作。
- 颜色与表面：主色 `#1E3BFA`，链接 `#0F23D9`，语义蓝/橙/红/绿、文字、边框和浅底均使用原型 token；卡片与弹窗为 4/6/8px 圆角，无旧红色主题、玻璃渐变、大圆角或深蓝按钮残留。
- 图标与图像：页面没有产品图片或装饰位图；图标统一使用 Lucide 描边图标，头像仅在真实列表行根据候选人姓名生成原型同类的彩色首字圆形标识，没有引入伪造业务图片。
- 文案与业务：保留系统真实字段、状态和接口；将原型“全部标签”明确为后端实际筛选的“全部推荐岗位”，搜索提示也改为接口真实覆盖的姓名、手机号、邮箱、公司和岗位，未复制原型示例姓名或技能。

### 页面、弹窗与交互

- 已移除人才库旧模块顶部的返回、重复标题、组织切换和新增招聘需求；页内只保留原型式标题、上传简历和刷新。
- 六类指标默认选中“总候选人”，已实际验证“待处理”点击筛选、再次点击恢复全部；来源筛选包含“其他”，推荐岗位和排序保留服务端分页查询。
- 已实际验证高级筛选展开/收起、重置入口、空数据、批量操作禁用态和上传简历完整弹窗；上传弹窗仍复用候选人模块的三种上传模式、来源、重复策略、城市和批次流程。
- 分组表格按实际状态生成 AI 识别、识别中、待处理和人才库中分组；保留分组全选、批量分配、批量重新识别、批量删除、单人分配、重新识别、停止匹配和查看。
- 人才详情旧断路已修复：人才库现在挂载独立的 720px 右侧抽屉，并使用人才库专属只读详情接口；只有 `matching / unmatched / talent_pool` 状态可由人才库查看权限读取，普通候选人详情权限未被放宽。
- 应用内浏览器 `error / warn / warning` 日志为 0；开发服务运行日志无编译或请求错误。

### 比较历史

- 预检查 P2：1394px 桌面视口下，页面级最小宽度会造成整体横向滚动并让右侧持续操作落出视口。已移除页面级最小宽度、允许筛选工具栏按需换行，并把最小宽度限制在分组列表内部。
- 修复后证据：`.design-qa/talent-pool/01-talent-pool-implementation.png` 与 `.design-qa/talent-pool/03-talent-pool-focus-comparison.png`。再次同屏检查未发现可执行的 P0/P1/P2 差异。
- 接受差异：共享框架的实际侧栏宽度与原型裁切存在轻微偏差，该框架已在前序模块单独验收；“全部推荐岗位”是为保持真实业务语义而保留的文案差异。
- 新版详情复核已使用现有真实人才数据打开详情、分配岗位弹窗并验证取消路径；重新识别、停止匹配继续复用既有接口和状态刷新，未为视觉验证改写候选人归属或岗位。

### 人才库验证

- `npm run build`：通过。
- `npm run typecheck`：通过。
- 人才库相关前端文件 ESLint（errors only）：通过，0 error。
- `fastApiProject/venv/bin/python -m pytest tests/test_permission_e2e.py::test_functional_permission_points_gate_direct_api_calls -q`：通过，1 passed。
- `git diff --check`：通过。

## 2026-07-12 候选人与人才库新版详情复核

### 参考与运行状态

- 参考原型：`AI Recruitment Redesign Project/候选人 Candidates.dc.html`、`AI Recruitment Redesign Project/人才库 TalentPool.dc.html` 及根目录设计说明。
- 实现地址：`http://localhost:3000/`；桌面端浅色模式。
- 参考截图为 1253 × 705，实际应用截图为 1207 × 679；组合图保持两张截图原始像素，不对实现截图拉伸。候选人最终实现截图为 `.design-qa/candidate-talent-details-v2/07-candidate-actual-final.png`。
- 完整视觉证据：`.design-qa/candidate-talent-details-v2/08-candidate-comparison-final.jpg`；人才库详情证据：`.design-qa/candidate-talent-details-v2/06-talent-comparison.jpg`。

### 候选人右侧详情

- 按新版原型重构为 760px 单栏右侧抽屉，顶部保留真实候选人身份、状态、匹配度和权限内业务操作，主导航统一为“档案 / 简历 / AI 评估 / 面试准备”。
- 档案保留姓名、联系方式、公司、年限、学历、年龄、城市、候选人编号、来源、更新时间、负责人、邮件记录、岗位、AI 岗位识别、标签备注、状态历史、流程操作和跟进记录；原型未覆盖字段按同一三列信息规则扩展。
- 简历保留标准、原始、历史三种视图，完整展示多段教育/工作经历；当前简历与所选历史版本语义分离，历史版本保留查看、下载和删除确认。
- AI、评审、面试题、面试安排、Offer、背调与跟进继续使用现有真实接口、推导状态、`visible_sections` 和 `round_index`；管理、流程执行、面试和邮件操作均按细粒度权限显示。
- “查看简历”快捷入口会直接落到简历页；列表中的详情型主操作会落到对应业务面板，不会被候选人切换初始化覆盖。
- 新版顶部“更多”承接刷新、打印和新窗口打开简历，避免旧工具栏隐藏后造成业务入口丢失。

### 人才库查看详情

- 按新版原型实现独立 720px 人才详情抽屉，不再复用候选人详情；包含真实 AI 识别结果、转岗潜力、完整基本信息、摘要/原始简历和底部持续操作。
- 当前真实数据为 1 条未匹配人才；已实际从“查看”打开详情，字段与专属接口返回一致，并验证分配岗位弹窗的打开、取消和父详情保留。
- 重新识别、停止匹配、分配岗位仅在候选人管理权限下显示；原始简历仍遵循现有后端下载权限，人才库查看权限未越权放宽。

### 问题与迭代

- P1：真实超长候选人姓名会挤压顶部操作——限制身份区宽度并允许操作区整体换行，按钮文字不拆分。
- P1：候选人详情内提交评审等二级弹窗关闭时会连带关闭父抽屉——父详情阻止外部交互/ESC 误关闭，实际验证取消后父详情仍保留。
- P1：面试题生成入口在新版结构中不可见——在“面试准备”中恢复生成、重新生成、停止生成和权限内下载。
- P1：Offer/跟进、面试题下载、面试安排，以及候选人行/批量状态操作曾对权限不足账号暴露——按后端与 Next 代理实际权限点补齐前端门控。
- P1：历史简历预览顶部仍显示当前文件——改为所选版本文件名、格式、大小和解析状态。
- P1：历史版本缺少删除入口——复用原有删除确认并保留管理权限约束。
- 接受差异：实际候选人字段和名称比原型示例更多、更长，因此档案信息比参考首屏更高；颜色、字号、间距、4/6/8px 圆角、边框、按钮和抽屉层级保持同一设计语言。

### 最终验证

- `npm run build`：通过。
- `npm run typecheck`：通过。
- 相关前端文件 ESLint（errors only）：通过，0 error。
- `fastApiProject/venv/bin/python -m pytest tests/test_permission_e2e.py::test_functional_permission_points_gate_direct_api_calls -q`：通过，1 passed。
- `git diff --check`：通过。
- 浏览器实际验证：候选人档案/简历/历史版本、部门评审与评审人选择、取消后父详情保留、人才库详情与分配岗位取消路径均可用；最后一轮无新增运行时 error/warning。

final result: passed
